__pycache__/
*.pyc
dist/
build/
*.spec
# never commit well data
*.pdf
*.csv
*.png
# ...except the bundled example chart (test data provided for this purpose)
!examples/example-flattened-chart.png

# Vercel CLI writes a short-lived OIDC token here; it must never be tracked.
.env.local
.env*.local
**/.env.local
