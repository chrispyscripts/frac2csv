**The continuous chart cannot appear as a stage any more.**

It was still turning up as "Stage ?" at the end of the list — the whole job re-plotted end to end, 1,180 minutes, no stage number.

The filtering was correct but it lived in the callers, and there are four of them. Each one had to remember, and that is not a thing four call sites do reliably: the file-drop path and the list-drop path did it differently, and one of them shipped the chart anyway.

It is now done inside the one function that builds every stage list in the app. A caller that forgets gets the right answer regardless — handing it the raw, unfiltered read now returns 28 stages, not 29. The chart still comes back with Show, because that asks for it explicitly.

**A dropped file also said "29 stages" over a list of 28.** The list-drop path counted what the list holds; the file-drop path counted what the reader returned. They now agree.

**Full Changelog**: https://github.com/chrispyscripts/frac2csv/compare/v1.11.15...v1.11.16
