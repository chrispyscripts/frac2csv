**Timing: two stages that came back a minute long now read in full.** You flagged them as "not picking up stage 7" and "not picking up a stage again". Stage 7 now reads 205.7 minutes instead of 1.0 and stage 29 reads 54.9 instead of 1.5, and 258 minutes of treatment data that were being dropped from that one well are exported again. The recovered stage 7 agrees with its own chemical chart on the same page, which reads 205.97 — two charts of one stage keeping the same clock.

**Overlap: a stage no longer hands its minutes to itself.** Where two charts cover the same minutes, the app gives those minutes to one of them so nothing is written twice. It decided which by the stage number printed on the next chart — and when a report prints the same stage on two pages, a chart could hand its whole run to the *other chart of its own stage* and be cut to a stub. A stage cannot re-plot itself, so that hand-over is now refused and both charts are kept as printed, overlap and all. Every genuine hand-over is unchanged: on the well above, 46 of the 48 stood and only the two bad ones were refused, and four other wells checked alongside it did not move at all.

**Full Changelog**: https://github.com/chrispyscripts/frac2csv/compare/v1.11.3...v1.11.4
